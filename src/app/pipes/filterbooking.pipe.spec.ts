import { FilterbookingPipe } from './filterbooking.pipe';

describe('FilterbookingPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbookingPipe();
    expect(pipe).toBeTruthy();
  });
});
